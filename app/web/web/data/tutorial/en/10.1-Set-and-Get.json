{"title":"10.1 Set and Get","blocks":[{"type":"heading","level":1,"text":"Set and Get"},{"type":"prose","html":"<p>Sonic Pi has a global memory store called Time State. The two main\nthings you do with it are to <code>set</code> information and <code>get</code>\ninformation. Let’s dive deeper…</p>"},{"type":"heading","level":2,"text":"Set"},{"type":"prose","html":"<p>To store information into the Time State we need two things:</p>"},{"type":"list","ordered":true,"items":["the information we want to store,","a unique name (key) for the information."]},{"type":"prose","html":"<p>For example, we might want to store the number <code>3000</code> with the key\n<code>:intensity</code>. This is possible using the <code>set</code> function:</p>"},{"type":"code","source":"set :intensity, 3000","runnable":true},{"type":"prose","html":"<p>We can use any name for our key. If information has already been stored\nwith that key, our new <code>set</code> will override it:</p>"},{"type":"code","source":"set :intensity, 1000\nset :intensity, 3000","runnable":true},{"type":"prose","html":"<p>In the above example, as we stored both numbers under the same key, the\nlast call to <code>set</code> ‘wins’, so the number associated with <code>:intensity</code>\nwill be <code>3000</code> as the first call to <code>set</code> is effectively overridden.</p>"},{"type":"heading","level":2,"text":"Get"},{"type":"prose","html":"<p>To fetch information from the Time State we just need the key we used to\n<code>set</code> it, which in our case is <code>:intensity</code>. We then just need to call\n<code>get[:intensity]</code> which we can see by printing out the result to the\nlog:</p>"},{"type":"code","source":"print get[:intensity] #=> prints 3000","runnable":false},{"type":"prose","html":"<p>Notice that calls to <code>get</code> can return information that was <code>set</code> in\na previous run. Once a piece of information has been <code>set</code> it is\navailable until either the information is overridden (just like we\nclobbered the <code>:intensity</code> value of <code>1000</code> to <code>3000</code> above) or Sonic Pi\nis closed.</p>"},{"type":"heading","level":2,"text":"Multiple Threads"},{"type":"prose","html":"<p>The main benefit of the Time State system is that it can be safely used\nacross threads or live loops. For example, you could have one live loop\nsetting information and another one getting it:</p>"},{"type":"code","source":"live_loop :setter do\n  set :foo, rrand(70, 130)\n  sleep 1\nend\n\nlive_loop :getter do\n  puts get[:foo]\n  sleep 0.5\nend","runnable":true},{"type":"prose","html":"<p>The nice thing about using <code>get</code> and <code>set</code> across threads like this is\nthat it will always produce the same result every time you hit run. Go\non, try it. See if you get the following in your log:</p>"},{"type":"code","source":"{run: 0, time: 0.0}\n └─ 125.72265625\n\n{run: 0, time: 0.5}\n └─ 125.72265625\n\n{run: 0, time: 1.0}\n └─ 76.26220703125\n\n{run: 0, time: 1.5}\n └─ 76.26220703125\n\n{run: 0, time: 2.0}\n └─ 114.93408203125\n\n{run: 0, time: 2.5}\n └─ 114.93408203125\n\n{run: 0, time: 3.0}\n └─ 75.6048583984375\n\n{run: 0, time: 3.5}\n └─ 75.6048583984375","runnable":true},{"type":"prose","html":"<p>Try running it a few times - see, it’s the same every time. This is\n what we call deterministic behaviour and it’s really very important\n when we want to share our music as code and know that the person\n playing the code is hearing exactly what we wanted them to hear (just\n like playing an MP3 or internet stream sounds the same for all\n listeners).</p>"},{"type":"heading","level":2,"text":"A Simple Deterministic State System"},{"type":"prose","html":"<p>Back in Section 5.6 we discussed why using variables across threads can\nlead to random behaviour. This stops us from being able to reliably\nreproduce code such as this:</p>"},{"type":"code","source":"## An Example of Non-Deterministic Behaviour\n## (due to race conditions caused by multiple\n## live loops manipulating the same variable\n## at the same time).\n##  \n## If you run this code you'll notice\n## that the list that's printed is\n## not always sorted!\na = (ring 6, 5, 4, 3, 2, 1)\n\nlive_loop :shuffled do\n  a = a.shuffle\n  sleep 0.5\nend\n\nlive_loop :sorted do\n  a = a.sort\n  sleep 0.5\n  puts \"sorted: \", a\nend","runnable":true},{"type":"prose","html":"<p>Let’s take a look at how this might look using <code>get</code> and <code>set</code>:</p>"},{"type":"code","source":"## An Example of Deterministic Behaviour\n## (despite concurrent access of shared state)\n## using Sonic Pi's new Time State system.\n##\n## When this code is executed, the list that's\n## printed is always sorted!\nset :a, (ring 6, 5, 4, 3, 2, 1)\n\nlive_loop :shuffled do\n  set :a, get[:a].shuffle\n  sleep 0.5\nend\n\nlive_loop :sorted do\n  set :a, get[:a].sort\n  sleep 0.5\n  puts \"sorted: \", get[:a]\nend","runnable":true},{"type":"prose","html":"<p>Notice how this code is pretty much identical to the version using\na variable before it. However when you run the code, it behaves as you\nwould expect with any typical Sonic Pi code - <em>it does the same thing\nevery time</em> in this case thanks to the Time State system.</p>"},{"type":"prose","html":"<p>Therefore, when sharing information across live loops and threads, use\n<code>get</code> and <code>set</code> instead of variables for deterministic, reproducible\nbehaviour.</p>"}]}
