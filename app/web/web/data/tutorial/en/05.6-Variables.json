{"title":"5.6 Variables","blocks":[{"type":"heading","level":1,"text":"Variables"},{"type":"prose","html":"<p>A useful thing to do in your code is to create names for things. Sonic\nPi makes this very easy: you write the name you wish to use, an equal\nsign (<code>=</code>), then the thing you want to remember:</p>"},{"type":"code","source":"sample_name = :loop_amen","runnable":true},{"type":"prose","html":"<p>Here, we’ve ‘remembered’ the symbol <code>:loop_amen</code> in the variable\n<code>sample_name</code>. We can now use <code>sample_name</code> everywhere we might have\nused <code>:loop_amen</code>. For example:</p>"},{"type":"code","source":"sample_name = :loop_amen\nsample sample_name","runnable":true},{"type":"prose","html":"<p>There are three main reasons for using variables in Sonic Pi:\ncommunicating meaning, managing repetition and capturing the results\nof things.</p>"},{"type":"heading","level":2,"text":"Communicating Meaning"},{"type":"prose","html":"<p>When you write code it’s easy to just think you’re telling the computer\nhow to do stuff - as long as the computer understands it’s OK. However,\nit’s important to remember that it’s not just the computer that reads\nthe code. Other people may read it too and try to understand what’s going\non. Also, you’re likely to read your own code in the future and try to\nunderstand what’s going on. Although it might seem obvious to you now -\nit might not be so obvious to others or even your future self!</p>"},{"type":"prose","html":"<p>One way to help others understand what your code is doing is to write\ncomments (as we saw in a previous section). Another is to use meaningful\nvariable names. Look at this code:</p>"},{"type":"code","source":"sleep 1.7533","runnable":true},{"type":"prose","html":"<p>Why does it use the number <code>1.7533</code>? Where did this number come from?\nWhat does it mean? However, look at this code:</p>"},{"type":"code","source":"loop_amen_duration = 1.7533\nsleep loop_amen_duration","runnable":true},{"type":"prose","html":"<p>Now, it’s much clearer what <code>1.7533</code> means: it’s the duration of the\nsample <code>:loop_amen</code>! Of course, you might say why not simply write:</p>"},{"type":"code","source":"sleep sample_duration(:loop_amen)","runnable":true},{"type":"prose","html":"<p>Which, of course, is a very nice way of communicating the intent of the\ncode.</p>"},{"type":"heading","level":2,"text":"Managing Repetition"},{"type":"prose","html":"<p>Often you see a lot of repetition in your code and when you want to\nchange things, you have to change it in a lot of places. Take a look at\nthis code:</p>"},{"type":"code","source":"sample :loop_amen\nsleep sample_duration(:loop_amen)\nsample :loop_amen, rate: 0.5\nsleep sample_duration(:loop_amen, rate: 0.5)\nsample :loop_amen\nsleep sample_duration(:loop_amen)","runnable":true},{"type":"prose","html":"<p>We’re doing a lot of things with <code>:loop_amen</code>! What if we wanted to\nhear what it sounded like with another loop sample such as\n<code>:loop_garzul</code>? We’d have to find and replace all <code>:loop_amen</code>s with\n<code>:loop_garzul</code>. That might be fine if you have lots of time - but what\nif you’re performing on stage? Sometimes you don’t have the luxury of\ntime - especially if you want to keep people dancing.</p>"},{"type":"prose","html":"<p>What if you’d written your code like this:</p>"},{"type":"code","source":"sample_name = :loop_amen\nsample sample_name\nsleep sample_duration(sample_name)\nsample sample_name, rate: 0.5\nsleep sample_duration(sample_name, rate: 0.5)\nsample sample_name\nsleep sample_duration(sample_name)","runnable":true},{"type":"prose","html":"<p>Now, that does exactly the same as above (try it). It also gives us\nthe ability to just change one line <code>sample_name = :loop_amen</code> to\n<code>sample_name = :loop_garzul</code> and we change it in many places through\nthe magic of variables.</p>"},{"type":"heading","level":2,"text":"Capturing Results"},{"type":"prose","html":"<p>Finally, a good motivation for using variables is to capture the results\nof things. For example, you may wish to do things with the duration of a\nsample:</p>"},{"type":"code","source":"sd = sample_duration(:loop_amen)","runnable":true},{"type":"prose","html":"<p>We can now use <code>sd</code> anywhere we need the duration of the <code>:loop_amen</code>\nsample.</p>"},{"type":"prose","html":"<p>Perhaps more importantly, a variable allows us to capture the result\nof a call to <code>play</code> or <code>sample</code>:</p>"},{"type":"code","source":"s = play 50, release: 8","runnable":true},{"type":"prose","html":"<p>Now we have caught and remembered <code>s</code> as a variable, which allows us\nto control the synth as it is running:</p>"},{"type":"code","source":"s = play 50, release: 8\nsleep 2\ncontrol s, note: 62","runnable":true},{"type":"prose","html":"<p>We’ll look into controlling synths in more detail in a later section.</p>"},{"type":"heading","level":2,"text":"Warning: Variables and Threads"},{"type":"prose","html":"<p>Whilst variables are great for giving things names and capturing the\nresults of things, it is important to know that they should typically\nonly be used locally within a thread. For example, <em>don’t do this</em>:</p>"},{"type":"code","source":"a = (ring 6, 5, 4, 3, 2, 1)\n\nlive_loop :sorted do\n  a = a.sort\n  sleep 0.5\n  puts \"sorted: \", a\nend\n\nlive_loop :shuffled do\n  a = a.shuffle\n  sleep 0.5\nend","runnable":true},{"type":"prose","html":"<p>In the above example we assign a ring of numbers to a variable <code>a</code> and\nthen used it within two separate <code>live_loop</code>s. In the first live loop\nevery <code>0.5</code>s we sort the ring (to <code>(ring 1, 2, 3, 4, 5, 6)</code>) and then\nprint it out to the log. If you run the code, you’ll find that the\nprinted list <em>is not always sorted!</em>. This may surprise you - especially\nthat sometimes the list is printed as sorted, and sometimes it is\nnot. This is called non-deterministic behaviour and is the result of a\nrather nasty problem called a race-condition. The problem is due to the\nfact that the second live loop is also manipulating the list (in this\ncase shuffling it) and by the time the list is printed, sometimes it has\njust been sorted and sometimes it has just been shuffled. Both live\nloops are racing to do something different to the same variable and\nevery time round a different loop ‘wins’.</p>"},{"type":"prose","html":"<p>There are two solutions to this. Firstly, <em>don’t use the same variable\nin multiple live loops or threads</em>. For example, the following code will\nalways print a sorted list as each live loop has its own separate\nvariable:</p>"},{"type":"code","source":"live_loop :shuffled do\n  a = (ring 6, 5, 4, 3, 2, 1)\n  a = a.shuffle\n  sleep 0.5\nend\n\nlive_loop :sorted do\n  a = (ring 6, 5, 4, 3, 2, 1)\n  a = a.sort\n  sleep 0.5\n  puts \"sorted: \", a\nend","runnable":true},{"type":"prose","html":"<p>However, sometimes we do want to share things across threads. For\nexample, the current key, BPM, synth etc. In these cases, the solution\nis to use Sonic Pi’s special thread-safe state system via the fns <code>get</code>\nand <code>set</code>. This is discussed later on in section 10.</p>"}]}
