# a built-in fn that makes a ring cannot be a variable's name: the program is refused before any of it runs
use_bpm 120   # at twice the tempo: specs/lang/preparser_refuses_fn_names.rb
play 60
scale = 3
